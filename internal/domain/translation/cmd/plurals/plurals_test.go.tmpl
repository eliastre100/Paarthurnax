// Code generated by go generate; DO NOT EDIT.

package {{ .Package }}

import "testing"

func TestPluralEvaluatorsMatchRails(t *testing.T) {
	tests := []struct {
		locale string
		hint   int
		want   Key
	}{
{{- range $test := .Tests }}
		{locale: "{{ $test.Locale }}", hint: {{ $test.Hint }}, want: "{{ $test.Want }}"},
{{- end }}
	}

	for _, tt := range tests {
		t.Run(tt.locale, func(t *testing.T) {
			definition, ok := plurals[tt.locale]
			if !ok {
				t.Fatalf("no plural definition for locale %q", tt.locale)
			}

			if got := definition.Evaluator(tt.hint); got != tt.want {
				t.Errorf("plural key for hint %d = %q, want %q", tt.hint, got, tt.want)
			}
		})
	}
}
